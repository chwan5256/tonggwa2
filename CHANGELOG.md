# 변경 기록

버전은 `v주.부.수` 로 붙입니다.
**주** — 구조가 바뀜(폴더·파일 구성) · **부** — 차시나 기능이 늘어남 · **수** — 고침·다듬기

---

## v0.7.1 — 2026-08-26
`실관측 자료 확보 — 4차시 '출제 금지' 해제`

**세 파일을 실관측으로 채웠습니다. 예시 데이터와 빨간 경고가 사라졌습니다.**

| 파일 | 기간 | 개수 | 출처 |
|---|---|---|---|
| `data/nino34.csv` | 1995-01 ~ 2026-06 | 378 | NOAA CPC 공식 ONI 표 |
| `data/co2-annual.csv` | 1959 ~ 2025 | 67 | NOAA GML 마우나로아 |
| `data/gistemp-annual.csv` | 1880 ~ 2025 | 146 | NASA GISTEMP v4 |

**옮겨 적기 오류를 기계로 걸러냈습니다.** 이 자료가 곧 출제 근거가 되므로 눈으로 훑지 않았습니다.
- **Niño3.4** — `sst − anom`(= 평년값)을 같은 달끼리 모으면 최대 **0.16 ℃** 안에서만 움직입니다.
  CPC 가 5년마다 평년값을 갱신하기 때문에 나타나는 계단 모양이며, **한 자리라도 잘못 적으면 그 달만 튀어 즉시 걸립니다.**
  378개월 연속성 확인, 알려진 최대·최소 6개 전부 일치(1997-12 +2.37 · 2015-12 +2.59 · 2023-12 +1.99 · 2008-01 −1.76).
- **CO₂** — 서로 다른 두 출처의 사본과 **67개 행 전부 자릿수까지 대조**. 감소한 해 0개.
- **GISTEMP** — 146개 해 연속성, 2024년 최고(+1.28 ℃) 확인.

**바뀐 것**
- **4차시** 자료가 CPC 공식 **3개월 이동평균(ONI)** 이 되어 실제 엘니뇨 판정과 결이 같아졌습니다.
  화면 설명을 '월평균'에서 **'3개월 이동평균 · 표시된 달은 가운데 달'** 로 바로잡았습니다.
  공식 편차(`anom`)도 함께 실어, 학생이 스스로 정한 기준으로 구한 편차와 견줄 수 있습니다.
- **4차시** 판정 문구 수정 — 전에는 "간이 기준"이라고만 했으나, 이제 자료 자체가 3개월 이동평균이므로
  **실제와 다른 점은 '기준(평년값)을 학생이 정한다는 것' 하나**임을 정확히 밝힙니다.
- **2차시** CO₂ 그래프가 붙박이 14개 점(1960~2023)에서 **실관측 67개 연도(1959~2025)** 로 바뀌었습니다.
  파일을 못 읽으면 예전 붙박이 값으로 그려 수업이 멈추지 않습니다.
  **연평균이라 계절 톱니가 없다**는 단서를 그림 설명에 넣었습니다.

**지금 엘니뇨가 진행 중입니다.**
NOAA CPC 는 2026년 8월 현재 **엘니뇨 주의보**를 내고 있고 2026년 7월 Niño3.4 는 **+1.4 ℃** 입니다.
자료의 오른쪽 끝이 바로 그 신호라, 4차시를 **지나간 사건이 아니라 지금 일어나는 일**로 다룰 수 있습니다.

**출제 주의 메모 갱신** — '4차시 출제 금지' 두 항목을 `[해소]` 처리하고, 대신 두 가지를 새로 적었습니다.
① 값이 3개월 이동평균이고 표시 달이 가운데 달임을 지문에 밝힐 것
② 자료가 2026년 6월까지 있어 시험 범위 밖 사건이 보이므로 **2026년 사건 자체는 묻지 않을 것**

**아직 남은 것** — `gistemp-annual.csv` 는 확보했으나 **아직 화면에 그리지 않았습니다.**
시험 범위 문구가 'CO₂ 농도 변화와 지구 평균 기온 편차 그래프'이므로 둘을 나란히 놓아야 범위를 채웁니다. 다음 작업입니다.

## v0.7.0 — 2026-08-26
`반별 응답 · 진도 · 글자 크기 · 원터치 수업 시작 · 접속 QR`

> ⚠ **Apps Script 를 반드시 다시 배포해야 합니다** (ver 5).
> 옛 배포에서는 반별 보기·일괄 여닫이·반 목록이 잠기고 교사 화면이 그 사실을 알려 줍니다.

**학생 화면**
- **반·이름을 한 번만** 넣으면 모든 차시에 따라옵니다. PDF 파일 이름과 기록표에 자동으로 들어갑니다.
  급하면 **[나중에 넣을게요]** 로 지나갈 수 있습니다 — 복습하러 온 학생을 붙잡지 않습니다.
  **이름은 이 기기 밖으로 나가지 않습니다.** 구글로 가는 것은 반뿐입니다.
- **진도 표시** — 차시 카드에 `7 / 18 문항` 과 [이어서 →]. 다 풀면 [다시 보기 →].
- **글자 크기** [가][가+][가++] — 뒷자리·시력이 약한 학생용. 화면 확대와 달리 표·그래프가 흐트러지지 않습니다.
  인쇄할 때는 늘 보통 크기로 돌아가 A4 짜임을 지킵니다.
- 자료실 아래에 **접속 QR** — 친구에게 주소를 불러 줄 필요가 없습니다.

**교사 화면**
- **[수업 시작] 원터치** — 차시 열기 → 오늘 수업 지정 → 진행 화면까지 한 번에.
- **[전체 열기] · [전체 닫기] · [오늘 것만 열기]** — 서버에서 한 번에 써서 중간 상태가 생기지 않습니다.
- **반 목록** 편집 — 학생이 처음 들어올 때 고르는 반입니다.
- **접속 QR** — [크게 띄우기] 로 칠판에 띄웁니다.
- 진행 화면 응답판에서 **반별로 골라 보기**. 반 단추에 응답 수가 함께 보입니다.

**독립 2차 검토에서 나온 것 — 전부 처리**
- ★ **`ME.askOnce()` 를 아무 데서도 부르지 않아, 학생에게 반을 묻는 일이 실제로는 일어나지 않았습니다.**
  반별 응답이라는 기능 전체가 껍데기였습니다. 다섯 차시와 자료실에서 부르도록 연결.
- ★ **옛 배포에서 반 필터가 조용히 거짓말을 했습니다.** '3반'을 눌러도 전체 통계가 오고
  화면에는 '3반'이라 적혔습니다. → `stats` 응답도 버전을 확인하고, 옛 배포면 잠급니다.
- ★ **'(반 없음)' 응답이 집계에는 들어가고 단추에서는 빠져** 전체 수와 반별 합이 맞지 않았습니다. → 숨기지 않습니다.
- ★ **원터치가 절반만 성공해도 성공이라 말하고 진행 화면으로 넘어갔습니다.**
  → 걸음마다 확인하고, "차시는 열렸으나 오늘 수업 지정은 실패"처럼 어디까지 됐는지 그대로 알립니다.
- ★ **진도 분모가 다섯 차시 전부에서 실제보다 작아** 절반만 풀어도 '다 풀었습니다'가 떴습니다.
  (02차시는 28개 중 18개만 풀어도 완료) → 빈칸·카드·슬롯·객관식을 각각 세도록 바로잡음.
  검산: 18 / 28 / 18 / 23 / 20 — 화면의 실제 항목 수와 일치.
- **시트 수식 주입** — 학생이 서술형에 `=IMPORTXML(…)` 을 적으면 교사 계정 권한으로 실행될 수 있었습니다.
  → `= + - @` 로 시작하는 글은 앞에 작은따옴표를 붙여 글자로만 남깁니다.
- **학생이 쓴 문장이 주소만 알면 통째로 읽혔습니다.** → 문장은 비밀번호가 맞을 때만 돌려줍니다.
  선택지 분포는 막대그래프를 그려야 하므로 그대로 둡니다.
- **학생 기기에서 `T` 키로 진행 화면을 열어** 다른 학생의 문장을 볼 수 있었습니다. → 교사 통행증이 있을 때만 열립니다.
- **잠긴 차시에서 `T` 를 누르면** 절이 없어 예외가 나고 화면이 빈 채로 멈췄습니다. → 들어가지 않습니다.
- **일괄 여닫이가 목록 밖 차시의 '닫힘' 기록을 지워** 주소를 아는 사람에게 열렸습니다. → 목록에 있는 것만 손댑니다.
- **'오늘 것만 열기'** 가 확인 없이 나머지를 전부 닫았습니다. → 무엇을 남기는지 이름을 보여 주고 묻습니다.
- **글자 크기가 정작 문항·선택지·해설에는 걸리지 않았습니다**(본문 문단만 커짐). → 학생이 읽는 여섯 곳에 연결.
- `act:change` 가 '배열의 마지막 = 방금 바뀐 것'으로 잘못 가정했습니다. → 바뀐 항목을 함께 실어 보냅니다.
- [크게 띄우기]가 주소가 길면 죽은 것처럼 보였고, Esc 처리기가 쌓였습니다. → 둘 다 수정.
- 자료실만 이스케이프를 하지 않았습니다. → 교사 화면과 같은 방식으로 통일.
- 학생이 반을 바꿔도 이미 보낸 답은 옛 반으로 남는다는 사실을 학생 화면에서 알리지 않았습니다. → 한 줄 추가.

**QR 인코더를 직접 만들었습니다 (`shared/qr.js`)**
외부 라이브러리를 부르면 학교 망에서 막힐 수 있어 규격대로 구현했습니다. 바이트 모드·정정 수준 M·버전 1~10.
**눈으로 보고 판단하지 않았습니다.** 만드는 도중 실제로 두 번 틀렸고, 둘 다 기계 검증으로 잡았습니다.
- **형식 정보 두 번째 사본의 행과 열을 맞바꿔 놓았습니다.** 자유 칸이 하나 늘어 데이터가 통째로 한 비트씩 밀렸고,
  **어떤 QR 도 읽히지 않았습니다.** 참조 구현과 부호어를 대조해 찾았습니다.
- **'마스크 2 가 나쁘다'고 잘못 결론지었습니다.** 표본 하나에서 마스크 2 만 실패하기에 빼 보았더니,
  다른 자료에서는 마스크 0 이 실패하고 마스크 2 는 읽혔습니다. 나쁜 마스크가 있는 게 아니라
  읽는 쪽(OpenCV 기본 검출기)이 자료에 따라 약한 것이었습니다. **표본 하나로 규칙을 만들면 안 됩니다** — 되돌렸습니다.
- 최종 검증: 길이 1~213자 + 한글 UTF-8 = **216가지**를 ① 규격대로 되읽어 리드-솔로몬 신드롬 0 확인,
  ② 휴대전화 스캐너 계열(ZXing)로 판독 — **둘 다 실패 0**.

**검증**
- 7개 화면 × 밝은/어두운 = 14개 + 새 화면(반·이름 창, 진도, 반 칩, QR, 응답판) — **대비 위반 0**
- 브라우저 자동 시험 40여 항목 통과, pageerror 0

## v0.6.0 — 2026-08-26
`교사 화면 2단계 입장 · 오늘 수업 지정 · 진행 화면 [← 자료실]`

**새로 생긴 것**
- **교사 화면 입장 문** — ① 허용 계정 확인 ② 비밀번호. 둘 다 통과해야 열립니다.
  기본 계정은 `cadrical@gmail.com` · `chwan5256@namkang.sen.hs.kr`, 기본 비밀번호는 `ai2026`.
- **계정 관리** — 교사 화면에서 계정을 더하고 고치고 지웁니다. 최소 한 개는 남습니다.
- **비밀번호 바꾸기** — 여섯 글자 이상. 바꾸면 예비 지문(SHA-256)을 화면에 띄워
  `teacher.html` 에 옮겨 적도록 안내합니다.
- **오늘 수업 지정** — 학생 자료실 맨 위 카드에 올릴 차시를 교사가 고릅니다.
  '자동'으로 두면 열린 차시 중 마지막. 해당 차시 카드에도 표시가 붙습니다.
- **진행 화면(#t) [← 자료실]** — 첫 화면으로 돌아갈 길이 없던 것을 고쳤습니다.
- **12시간 기억 / [잠그기]** — 기억해 둔 통행증도 새로고침마다 서버에 다시 확인합니다.
  다른 기기에서 비밀번호를 바꾸거나 계정을 빼면 이 브라우저도 곧바로 다시 묻습니다.
- **비밀번호 빗장** — 한 시간에 열 번 틀리면 십 분 동안 받지 않습니다.
- `README.md` — 배포·로그인 연결·수업 전 확인 절차.

**고친 것 — 독립 2차 검토에서 나온 것들**
- ★ **옛 배포에서 입장 문 두 단계가 모두 뚫리던 문제.**
  `doGet` 의 `default` 가 모르는 요청을 응답 집계로 흘려보내 `ok:true` 를 돌려주었고,
  페이지가 그것을 '계정 맞음 · 비밀번호 맞음'으로 읽었습니다.
  → 모르는 요청은 분명히 거절하고, 모든 응답에 **`mode`(어떤 요청의 답인지)와 `ver`(배포 버전)** 을 실어
  둘이 맞을 때만 믿습니다. '아니라는 표시'는 옛 배포가 붙여 주지 않으므로 **'맞다는 표시'로** 가릅니다.
- ★ **같은 이유로 닫아 둔 차시가 학생에게 열리던 문제** → `state` 응답을 검증하도록 수정.
- ★ **계정 목록을 못 읽었을 때 예비 목록을 서버 목록인 것처럼 보여 주던 문제.**
  그대로 저장하면 동료 계정이 조용히 사라졌습니다. → 경고를 띄우고 저장 단추를 잠급니다.
- **연결 실패인데 “비밀번호가 맞지 않습니다”** 라고 알리던 문제.
  → 맞음·틀림·**물어보지 못함** 세 갈래를 갈라 각각 다른 문구로.
  덕분에 예비 비밀번호 경로가 실제로 동작하게 됐습니다(전에는 도달 자체가 불가능했습니다).
- **'오늘 수업' 저장 실패가 화면에 남지 않던 문제** — 실패해도 “저장하는 중…”에 멈춰 있었습니다.
- **닫힌 차시를 오늘 수업으로 두면** 학생 화면이 말없이 다른 차시를 띄우던 것 → 교사 화면에 상시 경고.
- **[다시 확인] 이 편집 중인 계정 칸을 덮어쓰던 문제** → 확인을 먼저 묻습니다.
- **2단계가 흐릿하게만 보이고 키보드로는 입력되던 문제** → `fieldset disabled` 로 실제로 잠급니다.
- **낭독기에 단계 진행이 전해지지 않던 문제** → `role="dialog"`, `aria-live`, 초점 이동,
  문이 열린 동안 뒤쪽 본문에 `inert`.
- 학생 첫 화면이 구글 응답을 최대 6초 기다리던 것 → **manifest 로 먼저 그리고 나중에 다시 그립니다.**
  학교 망에서 구글이 막혀도 첫 화면이 멈추지 않습니다.
- 서버 값이 빈 문자열('자동')일 때 manifest 값으로 되돌아가던 어긋남 → 서버가 답했으면 서버가 정본.
- `manifest.json` 을 못 읽었을 때 교사 화면이 빈 채로 남던 것 → 안내를 띄웁니다.
- 화면에 넣는 모든 문자열을 이스케이프. 비밀번호 앞뒤 공백 처리를 서버와 맞춤.

**대비(對比) 전면 재검사**
검사 스크립트의 **색 파싱 자체가 틀려** 그동안의 '위반 0'이 잘못된 결과였습니다.
`color(srgb …)` 는 0~1, `rgb(…)` 는 0~255 인데 섞어 읽었고, 반투명 배경을 합성하지 않았습니다.
고쳐서 다시 재니 실제 위반이 나왔습니다.
- `--ink-faint` 밝은 화면 `#7D858C` → **`#656D74`** (3.75 → 5.26), 어두운 화면 `#7F878E` → **`#949BA1`**
- 계열색을 **작은 글자에 쓸 때만** 쓰는 `--s1-ink` · `--s2-ink` 신설.
  슬라이더 읽는 값(`.ctrl .val`)이 2.76 이었습니다 — 학생이 화면에서 읽어야 할 바로 그 숫자입니다.
- 이제 7개 페이지 × 밝은/어두운 = **14개 화면 전부 위반 0**, 큰 글자 3:1 기준까지 나누어 적용.

## v0.5.2 — 2026-08-24
`과학 내용 전수 검토 — 오류 16건 수정`

**① 명백한 오류**
- **3차시** 대기 대순환 그림에서 **페렐 세포와 극세포의 순환 방향이 뒤집혀** 있었음.
  → 페렐 60° 상승·30° 하강, 극세포 60° 상승·극 하강으로 바로잡음
- **3차시** 위도별 에너지 수지 — SVG 곡선·JS 판정·축 라벨이 **서로 다른 위도**를 가리켜
  위도 50~60°가 ‘에너지 과잉’으로 표시되던 문제.
  → 곡선·음영·점·판정을 **하나의 식**에서 생성하도록 재작성, 교차 위도 38.4°로 확정
- **2차시** 편차 문항에 **복수 정답**(‘기준 연도의 값’도 늘 0이라 변하지 않음) → 선택지 교체

**② 오해 소지**
- **4차시** 엘니뇨 상태에서 **하강 기류가 페루 쪽에 그대로** 남아, 같은 화면의 ‘페루 강수 많음’과 충돌
  → 상승역을 동태평양으로 옮기고, 엘니뇨 구간에서는 고리를 감추고 안내 문구로 대체
- **2차시** 대기의 수지가 닫히지 않던 문제 → **대기 → 우주 방출** 화살표 추가 (12+58=70, 94+58=152)
- **2차시** ‘50+94=144로 딱 맞습니다’ → 대류·전도·숨은열이 빠진 복사만의 균형임을 명시
- **2차시** 100 단위와 W/m² 혼용 → 70 단위 ≈ 240 W/m² 로 다리 놓음
- **1차시** ‘일부는 우주로’ 라벨이 태양 복사 설명 위에 놓여 오독 → 위치·문구 수정
- **4차시** ‘반사율은 위도에 따라 크게 다르지 않다’(사실과 다름) → 해설 교체
- **4차시** 엘니뇨 판정이 실제 기준(3개월 이동평균)과 다름 → **간이 기준**임을 명시
- **5차시** 사막 분포를 하강 기류 하나로만 설명 → 대륙 내부·한류 연안·바람그늘 사막 언급
- **5차시** ‘하강역에 놓여 건조하다’는 사막화 원인이 아닌 배경 조건 → 카드 교체
- **3차시** ‘구형이라 방출량도 다르다’ → 흡수량 차이 → 기온 차이 → 방출량 차이로 분리

**③ 표기**
- **1차시** 온실효과 크기 33 ℃ / 34 ℃ 혼재 → **33 ℃** 통일, 없을 때 기온 −19 → **−18 ℃**
- **1차시** 온도 빈칸 오답에 태양 상수(1361 W/m²)를 K로 섞음 → 373 K 로 교체
- **1차시** 지구 복사 곡선이 ‘전파’ 라벨까지 뻗음 → 곡선 축소, 라벨 이동
- **1차시** 스펙트럼 두 곡선의 **높이를 서로 비교할 수 없음**을 명시
- **4차시** 데이터 카드 결측 문구가 뜻이 뒤집혀 있었음 → 수정
- **2차시** 모형 온도가 실제 값이 아님을 붉은 상자로 명시
- **1차시** 정리 확인 문항 1개 추가(파장과 온도의 관계)

## v0.5.1 — 2026-08-24
`성취기준 원문 표기 · PDF 안내 · 1~2차시 그림 재제작`

- **추가** 모든 차시와 자료실 머리말에 **성취기준 원문**과 ‘이 차시가 맡는 부분’ 표기
- **고침** [PDF 받기] — 기능은 정상이었으나 인쇄 창에서 **대상을 ‘PDF로 저장’으로 바꿔야** 하는 것을
  몰라 동작하지 않는 것처럼 보였음. 누르면 **두 단계 안내 창**이 먼저 뜨도록 바꿈.
  저장 파일 이름도 `차시명_이름` 으로 자동 지정
- **재제작** 1차시 온실 효과 그림 → **‘온실 기체 없음 / 지금의 지구’ 견주기**.
  재복사 화살표와 지표 온도(−19 ℃ ↔ 15 ℃)가 함께 바뀜
- **재제작** 2차시 「온실 효과를 일으키는 에너지의 양」 → **온실 기체 양 슬라이더**.
  지표 방출·대기 흡수·**재복사**·지표 기온이 함께 움직임.
  모형: `E = 50 / (1 − r·g)`, `A = g·E`, `R = r·A` (r = 94/132) —
  ‘지금의 대기’에서 유인물의 **144 · 132 · 94** 와 정확히 일치
- **변경** 2차시 열수지 두 번째 그림의 채우기 활동을 그림 읽기 기반 **빈칸**으로 교체

## v0.5.0 — 2026-08-24
`유인물 기준 1~5차시 전면 개편 · 조작 언어 통일 · 대비 수정`

- **구조** 차시를 유인물 순서에 맞춰 다시 나눔 (5차시)
  1. 복사 평형과 온실 효과 `u2/01-radiation/`
  2. 지구 열수지와 지구 온난화 `u2/02-budget-shift/`
  3. 대기 대순환과 해수의 순환 `u2/03-circulation/`  ← 신설
  4. 엘니뇨 `u2/04-el-nino/`
  5. 사막화와 단원 정리 `u2/05-desert/`  ← 신설
- **삭제** `u2/01-energy-budget/`, `u2/03-el-nino/` (내용은 위로 옮김)
- **내용** 「2026학년도 2학기 통합과학2_D(지구과학 파트)」 유인물의 괄호 항목을 전부 반영
  (복사 평형·태양/지구 복사·온실 효과·반사율 30%·열수지 144·132·94·온난화 원인·
   위도별 에너지 불균형·3개 순환 세포·표층 순환·엘니뇨 평상시/발생시·사막의 분포)
- **고침** 밝은 화면에서 글자가 배경과 같아 보이던 문제 — `--on-accent` 토큰 도입,
  링크 기본색 지정, 취약한 이중부정 CSS 규칙 제거
- **디자인** 빈칸·칩·카드·상자를 `.pill` 하나로 통일. 빈칸은 **드롭다운**으로 바뀜
- **추가** 화면 아래 고정 **[PDF 받기]** 막대 — 개념·그림·문항·내 답을 통째로 저장
- **추가** 교사 페이지 **수업 영상** 칸 — 유튜브 주소를 붙여 넣으면 저장. 코드 수정·재배포 불필요
- **추가** `Code.gs` 영상 주소 저장(`mode=vid` / `setvid`)

## v0.4.0 — 2026-08-24
`학생 활동 엔진 · PDF 저장 · 40분 타이머 · 2차시 추가`

- **추가** `shared/act.js` — 빈칸 채우기 / 그림 위에 놓기 / 카드 분류 / 서술형 자기점검 / 학습 기록
- **추가** 학생용 **PDF 저장** — 푼 문항과 채운 빈칸을 한 장으로. 기기 안에서만 만들어지며 전송하지 않음
- **추가** `u2/02-budget-shift/` 02차시 「평형점이 움직인다」
- **변경** 진행 화면 수업 시간 50분 → **40분** (출석·환기·기기 접속·마무리 10분은 따로)
- **추가** 진행 화면 전용 영상 자리 — `<div class="teach-only" data-yt="…">`
- **변경** `shared/lib.js` 객관식 결과를 학습 기록으로 넘김 (`tg:quiz` 이벤트)
- **변경** `shared/base.css` 활동·인쇄 스타일 추가

## v0.3.1 — 2026-08-24
`ghost 버튼 글자 안 보이던 것 수정`

- **고침** 테마별 `.btn` 글자색 규칙이 `.btn.ghost` 를 덮어써서 밝은 화면에서 버튼이 빈 상자로 보이던 문제

## v0.3.0 — 2026-08-24
`교사 페이지 분리 · 자료실 개편`

- **추가** `teacher.html` — 연결 상태 진단, 차시 여닫이, 진행 화면 바로가기
- **변경** 자료실을 대단원 → 중단원 → 차시 3단 구조로. 학교·학기·문의 정보 추가
- **변경** 차시 공개 기본값을 `manifest.json` 의 `open` 으로. 구글이 끊겨도 수업이 멈추지 않음

## v0.2.0 — 2026-08-24
`응답판 CORS 해결 · 절별 타이머 · 차시 여닫이`

- **고침** 교사 응답판이 안 뜨던 문제 — Apps Script 읽기를 JSONP 로 바꿈
- **추가** 절별 배정 시간(`data-min`)과 초과 알림
- **추가** `shared/gate.js` 차시 여닫이

## v0.1.0 — 2026-08-24
`3차시 · 교사 진행 화면`

- **추가** `u2/03-el-nino/` 엘니뇨 시뮬레이터 + 편차 실험실
- **추가** `shared/teach.js` 교사 진행 화면(`#t`)
- **추가** `apps-script/Code.gs` 익명 응답 수집
- **추가** `data/README.md` 실데이터 스냅샷 절차
